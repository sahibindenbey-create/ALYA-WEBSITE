"use client";
import Link from 'next/link'; import {Heart,ArrowRight} from 'lucide-react'; import {useStore} from '../store';
const money=n=>n==null?'Fiyat için iletişime geçin':new Intl.NumberFormat('tr-TR',{style:'currency',currency:'TRY',maximumFractionDigits:0}).format(n);
export default function Wishlist(){const {wishlist,toggleWishlist}=useStore(); return <main className="wishlist-page"><div className="collection-hero"><small>ALYA HOMES</small><h1>Favoriler</h1><p>{wishlist.length} ürün</p></div>{!wishlist.length?<div className="empty-state"><Heart size={38}/><h2>Henüz favoriniz yok.</h2><p>Beğendiğiniz ürünleri kalp simgesine dokunarak kaydedin.</p><Link className="button dark" href="/collections/all">Ürünleri keşfet <ArrowRight size={16}/></Link></div>:<div className="catalog-grid">{wishlist.map(p=><article className="catalog-card" key={p.slug}><Link href={`/products/${p.slug}`}><div className="catalog-image"><img src={p.image} alt={p.name}/></div><span className="code">{p.code}</span><h2>{p.name}</h2><p>{money(p.price)}</p></Link><button className="remove-wish" onClick={()=>toggleWishlist(p)}>Favorilerden çıkar</button></article>)}</div>}</main>}
