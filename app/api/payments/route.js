import sql from 'mssql';
export const runtime='nodejs'; export const dynamic='force-dynamic';
let poolPromise=null;
const configured=()=>Boolean(process.env.SQL_SERVER&&process.env.SQL_DATABASE&&process.env.SQL_USER&&process.env.SQL_PASSWORD);
async function pool(){if(!configured())return null;if(!poolPromise)poolPromise=sql.connect({server:process.env.SQL_SERVER,database:process.env.SQL_DATABASE,user:process.env.SQL_USER,password:process.env.SQL_PASSWORD,port:Number(process.env.SQL_PORT||1433),options:{encrypt:String(process.env.SQL_ENCRYPT||'false').toLowerCase()==='true',trustServerCertificate:String(process.env.SQL_TRUST_SERVER_CERTIFICATE||'true')}}).catch(e=>{poolPromise=null;throw e});return poolPromise}
export async function POST(req){try{const b=await req.json();const orderNo=String(b.orderNo||'').trim();const method=String(b.method||'').trim();if(!orderNo||!method)return Response.json({error:'orderNo ve method gerekli'},{status:400});if(!['Kart','Havale/EFT'].includes(method))return Response.json({error:'Geçersiz ödeme yöntemi'},{status:400});const p=await pool();if(!p)return Response.json({ok:true,source:'file',paymentStatus:'Bekliyor',provider:'demo',message:'SQL ödeme kaydı için bağlantı yapılandırılmamış.'});const o=await p.request().input('orderNo',sql.NVarChar(60),orderNo).query('SELECT TOP 1 Id,Total,PaymentMethod,PaymentStatus FROM dbo.AlyaOrders WHERE OrderNo=@orderNo');if(!o.recordset.length)return Response.json({error:'Sipariş bulunamadı'},{status:404});const order=o.recordset[0];const ref=`PAY-${Date.now().toString(36).toUpperCase()}`;const provider=method==='Havale/EFT'?'BankTransfer':(process.env.PAYMENT_PROVIDER||'NotConfigured');await p.request().input('orderId',sql.BigInt,order.Id).input('ref',sql.NVarChar(100),ref).input('provider',sql.NVarChar(60),provider).input('method',sql.NVarChar(60),method).input('amount',sql.Decimal(18,2),Number(order.Total)||0).query(`INSERT dbo.AlyaPaymentAttempts(OrderId,Reference,Provider,PaymentMethod,Amount,Status) VALUES(@orderId,@ref,@provider,@method,@amount,N'Bekliyor')`);await p.request().input('orderNo',sql.NVarChar(60),orderNo).input('provider',sql.NVarChar(50),provider).input('reference',sql.NVarChar(120),ref).query(`UPDATE dbo.AlyaOrders SET PaymentProvider=@provider,PaymentReference=@reference,PaymentStatus=N'Bekliyor',UpdatedAt=SYSUTCDATETIME() WHERE OrderNo=@orderNo`);return Response.json({ok:true,source:'sql',reference:ref,provider,status:'Bekliyor',requiresProvider:provider==='NotConfigured',message:provider==='NotConfigured'?'Kart ödemesi için ödeme sağlayıcısı henüz yapılandırılmadı.':method==='Havale/EFT'?'Havale/EFT talimatı sipariş detayında gösterilebilir.':'Ödeme sağlayıcısına yönlendirme hazır.'})}catch(e){return Response.json({error:e?.message||'Ödeme oturumu oluşturulamadı'},{status:409})}}
