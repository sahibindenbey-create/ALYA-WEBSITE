"use client";
import Link from "next/link"; import { Minus, Plus, Trash2, ArrowRight } from "lucide-react"; import { useStore } from "../store";
const money=n=>new Intl.NumberFormat("tr-TR",{style:"currency",currency:"TRY",maximumFractionDigits:0}).format(n);
export default function CartPage(){const {cart,total,changeQty,remove}=useStore(); return <main className="cart-page"><div className="cart-title"><small>ALYA HOMES</small><h1>Sepetiniz</h1></div>{!cart.length?<div className="empty-cart-page"><h2>Sepetiniz henüz boş.</h2><Link href="/collections/all" className="button dark">Alışverişe devam et <ArrowRight size={17}/></Link></div>:<div className="cart-layout"><section>{cart.map(i=><article className="full-cart-item" key={i.slug}><div className="full-thumb"><img src={i.image} alt=""/></div><div className="full-info"><Link href={`/products/${i.slug}`}><h2>{i.name}</h2></Link><p>{money(i.price)}</p><div className="qty"><button onClick={()=>changeQty(i.slug,-1)}><Minus size={13}/></button><span>{i.qty}</span><button onClick={()=>changeQty(i.slug,1)}><Plus size={13}/></button></div></div><button onClick={()=>remove(i.slug)} aria-label="Ürünü sil"><Trash2 size={18}/></button></article>)}</section><aside className="cart-summary"><h2>Sipariş özeti</h2><div><span>Ara toplam</span><strong>{money(total)}</strong></div><div><span>Kargo</span><strong>{total>=1000?"Ücretsiz":money(99)}</strong></div><hr/><div className="grand"><span>Toplam</span><strong>{money(total+(total>=1000?0:99))}</strong></div><div style={{display:'flex',gap:10,marginTop:16}}><Link className="checkout" style={{flex:1}} href="/checkout">Ödemeye geç <ArrowRight size={17}/></Link><Link className="button dark" style={{flex:1,display:'flex',justifyContent:'center',alignItems:'center',gap:7}} href="/collections/all">Alışverişe devam et</Link></div></aside></div>}</main>}
